import MLKit


class Column:

    def __init__(self, name, values):
        self.name = name
        self.values = values
    
    def compute_data(self):
        for value in self.values:
            pass
